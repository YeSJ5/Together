{
  "name": "together-backend",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "build": "node -e \"console.log('TOGETHER backend does not require a separate build step.')\"",
    "dev": "node --watch src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.1",
    "livekit-server-sdk": "^2.13.2",
    "socket.io": "^4.8.1"
  }
}
